# API URL for the FastAPI backend
VITE_API_URL=http://localhost:8000

# Set to false to actually fetch from the backend. 
# Set to true to use the local mock data (useful if the backend isn't running).
VITE_USE_MOCK_DATA=false
